<!doctype html>
<html lang="de">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Anmelden | edoku</title>
    <link rel="stylesheet" href="/css/style.css">
  </head>
  <body>
    <main class="auth-page">
      <section class="auth-card">
        <div class="auth-brand">
          <img class="auth-logo" src="/img/logo.svg" alt="" width="48" height="48">
          <div>
            <p class="eyebrow">edoku</p>
            <h1>Anmelden</h1>
          </div>
        </div>
        <p class="auth-subtitle">Lokale Projekt- und Dokumentationsverwaltung.</p>
        <%- include("partials/flash", { flash }) %>
        <form action="/login" method="post" class="stack-form">
          <label>
            <span>E-Mail</span>
            <input type="text" name="email" autocomplete="username" required>
          </label>
          <label>
            <span>Passwort</span>
            <input type="password" name="password" autocomplete="current-password" required>
          </label>
          <button type="submit">Anmelden</button>
        </form>
        <p class="meta-line">Noch kein Konto? <a href="/register">Registrieren</a></p>
      </section>
    </main>
    <%- include("partials/footer") %>
    <script src="/js/app.js"></script>
  </body>
</html>
